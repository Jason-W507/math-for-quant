\section{组合构建、摩擦与可复现研究材料}
\label{ch:lower-multifactor-research}

\begin{itemize}
  \input{tex/generated/prerequisites/lower-multifactor-research}
\end{itemize}

\MFQTransition{模型输出不是策略收益}

完整链路必须写成
\[
x_{i,t}\longrightarrow \tilde x_{i,t}\longrightarrow q_{i,t}
\longrightarrow w_{i,t}\longrightarrow R^{\mathrm{gross}}_{t+1}
\longrightarrow R^{\mathrm{net}}_{t+1}.
\]
$\tilde x$ 是时点预处理后的信号，$q$ 是分组，$w$ 是实际仓位。若代码一边计算 IC，一边从 fixture 直接读取“策略收益”，模型与交易台账仍是两条互不相连的管线。

\input{tex/figures/generated/lower-mf-pipeline.tex}




\MFQTransition{分组、权重与重叠持有期}

先在每个调仓日用事先确定资产池排序，再把最低组做空、最高组做多。等权使每一侧绝对权重和为 1；市值权重则在组内按正市值归一化。两者回答不同问题，不能因为某一结果更好而事后选择。

若持有 $H$ 期，每个时点的有效仓位是仍存续 vintage 的平均：
\[
w_t=\frac1{|A_t|}\sum_{s\in A_t}w_t^{(s)},
\qquad A_t=\{s:t-H+1\le s\le t\}.
\]
这会平滑仓位并引入收益重叠；推断时需处理序列相关。资产退市、停牌或不可卖空不能靠删除行解决，必须进入可交易集合和退出规则。

\MFQTransition{换手、成本与容量}

双边换手定义为 $\tau_t=\sum_i|w_{i,t}-w_{i,t-1}|$。最小净收益台账为
\[
R^{\mathrm{net}}_{t+1}=w_t^{\mathsf T}r_{t+1}
-c\tau_t-\operatorname{impact}(w_t,\mathrm{ADV}_t,\sigma_t).
\]
事先确定两期等权例的毛收益分别为 $0.035$ 与 $0.030$，期均毛收益 $0.0325$；平均双边换手为 2，单位换手成本 $0.001$，容量冲击 $0.0005$，所以净收益为 $0.0300$。市值加权与两期持有给出不同仓位和净收益，这是一项预先声明的敏感性分析，不是择优汇报。

\input{tex/figures/generated/lower-mf-cost-chain.tex}



真实报告应拆出佣金、价差、冲击、借券、税费、参与率、成交完成率与资金规模。容量不是一个永久常数；应报告规模到净收益、参与率和成交失败的曲线。

\MFQTransition{两个错误研究材料}

\textbf{错误包 A：预测与收益断开。} 程序打印 AR、回归或 ML 预测，但 \texttt{trade\_return} 直接来自 fixture。审计步骤是追踪每一笔 $w_t$ 是否由事先确定预测生成，并用 $w_t r_{t+1}$ 独立重算。修复不是给报告补一句“模型驱动”，而是删除外生收益字段。

\textbf{错误包 B：选择后重写检验族。} 研究者测试 500 个信号，只把 20 个较好结果交给 BH，并把 $m$ 写成 20。审计需要恢复完整尝试账本、参数网格和失败结果；若无法恢复，该证据不能升级为样本外结论。

Capstone 固定交付：研究问题、数据与时间协议、手算小面板、预测或定价模型卡、透明与库实现、训练/选择/最终测试、分组组合、成本容量、两类故障注入、限制报告和一键复现命令。全球通用协议与 A 股涨跌停、停牌、T+1、融券可得性必须分层陈述。

事先确定路线报告由下面的命令生成，并与仓库中的期望报告逐字比较：
\begin{lstlisting}[language=bash]
uv run python tools/build_multifactor_report.py
\end{lstlisting}
报告源文件和期望文件分别登记在课程清单中。WDI 截面只验证外部数据责任；合成组合只验证算术；两者都不允许被描述为实盘业绩。

\begin{implementationnote}
运行 \path{notebooks/lower/multifactor_research.ipynb}。柱状图并列毛收益、成本、容量冲击和净收益；故意裁掉收益面板一列必须触发形状错位失败。
\end{implementationnote}

\MFQTransition{分组组合的逐步算例}

假设五只股票得分排序为 $A>B>C>D>E$，未来收益为 $(3\%,1\%,0\%,-1\%,-2\%)$。最简单多空组合做多 $A$、做空 $E$，毛收益为 $5\%$；若每侧取两只等权，则收益为
\[
\frac{3\%+1\%}{2}-\frac{-1\%-2\%}{2}=3.5\%.
\]
单只头尾更容易被个股异常主导，分组更稳但信号稀释。市值加权、风险加权和分数加权回答不同投资问题，不能在测试期择优。

若下一期排名变成 $E>A>C>B>D$，目标仓位变化决定换手。换手应从旧实际仓位到新目标仓位计算，部分成交时还要保留未完成订单。只按入选名单变化计数会忽略权重大小。

\MFQTransition{归因：模型收益究竟来自哪里}

组合收益可分为市场、行业、风格和特异部分。一个“质量 Alpha”若主要来自低波动或小盘暴露，应先说明它是复合暴露，而非把中性化后的剩余全部称为选股能力。归因至少按日期、行业、市值、流动性和多空两侧展开。

回撤归因比平均收益更能暴露失效：是信号翻转、拥挤、成本上升、股票池变化还是数据缺口？对每种失败预先定义诊断统计量，避免亏损后任意讲故事。

\MFQTransition{稳健性不是堆叠更多回测}

合理稳健性包括邻近参数、不同但事前合理的股票池、子时期、成本压力和数据供应商交叉核对。尝试越多，越要把全部结果计入选择权。热力图不能只截取正收益区域。

真正的最终测试是研究设计事先确定后一次性打开的未来区间。若失败，应记录失败并回到新版本开发；不能保留同一个“最终测试”反复修改直到通过。

\begin{diagnostic}
高 IC、单调分组和显著 t 值可以同时存在，但若信号换手过高、容量过低或依赖不可借券股票，仍不是可实施策略。统计证据和实施证据必须并列。
\end{diagnostic}

\section{本路线的综合项目}

把前面的模型、估计和组合构建合在一个小型研究中：先提出可证伪的因子问题，再用独立基准核对估计，最后在未参与选择的区间报告分组收益、成本、容量和限制。读者应保留一次失败结果，并说明它是由信息时点、选择偏差、交易摩擦还是数值不稳定造成的。这个项目用于练习研究判断，不代表生产部署或实盘业绩。

\input{tex/lower/chapters/multifactor-model-questions}
\input{tex/lower/chapters/multifactor-estimation-questions}
\input{tex/lower/chapters/multifactor-research-questions}
