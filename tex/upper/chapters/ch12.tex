\chapter{时间序列、预测与状态空间基础}
\label{ch:time-series}

\begin{introduction}
  \item 先修：第 10 章的估计与回归，第 11 章的随机过程与条件信息。
  \item 学习产出：能声明平稳与创新假设，推导 AR(1) 的矩和预测误差，用标量 Kalman 递推更新隐状态，并识别伪回归、结构变化与随机切分泄漏。
  \item 研究连接：统计套利、波动率预测、动态因子和执行信号都必须先说明时间索引、信息集与验证顺序。
\end{introduction}

\section{弱平稳把日历时间压缩成滞后}

过程 $(X_t)$ 若满足 $\mathbb E[X_t]=\mu$、$\operatorname{Var}(X_t)<\infty$，且
\[
  \gamma(h)=\operatorname{Cov}(X_t,X_{t-h})
\]
只依赖滞后 $h$ 而不依赖 $t$，称为弱平稳。白噪声 $(\varepsilon_t)$ 还满足零均值、常方差和不同期不相关；不相关不自动等于独立，金融收益中的波动聚集也不因线性自相关接近零而消失。

ARMA 模型把当前值写成过去状态和创新的有限线性组合：
\[
  X_t=c+\sum_{i=1}^{p}\phi_iX_{t-i}
      +\varepsilon_t+\sum_{j=1}^{q}\theta_j\varepsilon_{t-j}.
\]
创新是相对于过去信息不可预测的误差，不是任意残差。AR 根在单位圆外、MA 根在单位圆外分别对应常用的平稳与可逆条件；这里只用 AR(1) 建立可手算基线。

\section{AR(1) 的矩、相关与多步预测}

令 $X_t=\phi X_{t-1}+\varepsilon_t$，$|\phi|<1$，且创新方差为 $\sigma^2$。平稳解满足
\[
  \operatorname{Var}(X_t)=\frac{\sigma^2}{1-\phi^2},\qquad
  \gamma(h)=\phi^{|h|}\gamma(0).
\]
给定 $\mathcal F_t$，$h$ 步预测与误差方差为
\[
  \widehat X_{t+h\mid t}=\phi^hX_t,
  \qquad
  \operatorname{Var}(X_{t+h}-\widehat X_{t+h\mid t})
  =\sigma^2\sum_{j=0}^{h-1}\phi^{2j}.
\]
当 $\phi=0.8,\sigma^2=1$ 时，理论方差为 $2.777778$；从 $X_t=1$ 出发的三步预测为 $0.512000$，预测误差方差为 $2.049600$。固定种子长路径得到均值 $-0.013814$、方差 $2.764404$、一阶自相关 $0.799071$，只在声明容差内交叉验证解析式。

\section{状态空间模型把不可见状态与观测分开}

标量局部水平模型为
\[
  x_t=x_{t-1}+w_t,\quad w_t\sim\mathcal N(0,Q),
  \qquad y_t=x_t+v_t,\quad v_t\sim\mathcal N(0,R),
\]
并假设两类噪声跨期且彼此独立，也与初始状态独立。在这些线性高斯假设下，Kalman 滤波递推条件均值与条件方差；它先预测，再用新观测修正：
\[
  P_{t\mid t-1}=P_{t-1\mid t-1}+Q,
  \quad K_t=\frac{P_{t\mid t-1}}{P_{t\mid t-1}+R},
\]
\[
  x_{t\mid t}=x_{t-1\mid t-1}+K_t(y_t-x_{t-1\mid t-1}),
  \quad P_{t\mid t}=(1-K_t)P_{t\mid t-1}.
\]
从 $(x_{0\mid0},P_{0\mid0})=(0,1)$、$Q=0.25,R=1$ 出发，依次观测 $1,-0.5,0.25$，三次滤波均值依次为 $0.555556$、$0.084615$ 与 $0.152494$，末次增益为 $0.410431$。这些数可用分数逐步手算，程序不得把滤波输出再当成自己的 oracle。

\section{负例：时间顺序不是可随意打乱的行号}

两个独立随机游走在水平值上可能产生很高的 $R^2$，即伪回归；固定种子实验的水平回归 $R^2=0.344011$，差分回归 $R^2=0.000216$。高拟合度没有提供平稳残差、协整关系或经济机制。

再令 AR 系数在样本后段从 $0.85$ 变为 $-0.25$。随机打乱后切分把新制度信息泄漏进训练集，随机切分 MSE 为 $1.436939$，时间切分 MSE 为 $2.226153$。更小的随机切分误差不是更可信的未来预测；研究报告应给出滚动或扩展窗口、断点诊断、训练与决策时点以及数据可得性。

\input{tex/upper/chapters/ch12-questions}
\input{tex/upper/chapters/ch12-hints}

\noindent\textbf{本章小结与 Capstone 连接}\quad
平稳性规定哪些矩能跨时间复用，创新规定预测误差相对哪个信息集不可预测，状态空间递推把隐状态与测量噪声分开。上册 Capstone 将要求所有动态结论附带时间切分、结构变化诊断和失败边界。
