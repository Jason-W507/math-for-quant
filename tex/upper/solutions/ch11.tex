\chapter{第 11 章分层答案}
\label{app:ch11-solutions}

\section*{口述概念}
\begin{enumerate}[leftmargin=*]
  \item 有限维分布描述任意有限时点联合律；版本只要求每个固定时点几乎处处相等，不可区分要求除同一个零测集外整条路径相等。连续修改需要额外正则性定理，单点边缘律无法决定跨时依赖或路径粗糙度。
  \item 适应表示 $X_t$ 在 $\mathcal F_t$ 可知；可预测要求动作在发生前由过去信息决定；$\tau$ 是停止时刻要求 $\{\tau\le t\}$ 当时可判断。用当日最高价决定当日盘中成交是未来函数。
  \item $\pi P=\pi$ 只定义稳态。有限不可约链的稳态唯一且所有状态正常返；再加非周期才保证从任意初值收敛。交替链有稳态却没有极限分布，可约链则可能有多个闭类和多个稳态。
\end{enumerate}

\section*{笔试推导}
\begin{enumerate}[leftmargin=*,start=4]
  \item 递推 $2h_i=h_{i-1}+h_{i+1}$ 说明一阶差分为常数，边界给 $h_i=i/N$。而 $m_{i+1}-2m_i+m_{i-1}=-2$，二次解配合边界得 $m_i=i(N-i)$。取 $N=4,i=2$，赌徒破产命中概率与期望时长为 $0.500000$ 与 $4.000000$。
  \item 展开 $S_{n+1}^2=S_n^2+2S_n\xi_{n+1}+\xi_{n+1}^2$，条件期望消去交叉项并增加 1，故 $S_n^2-n$ 为鞅。加倍下注截断财富期望仍为 0；尾部损失贡献趋于 1，财富族非一致可积，无界停止不满足所用定理条件。
  \item 首次达到 $a$ 后反射路径，把“到过 $a$ 且终点低于 $a$”与“终点高于 $a$”配对，得 $2\{1-\Phi(a/\sqrt t)\}$。当 $a=t=1$，反射原理给出命中概率 $0.317311$。离散监控遗漏网格点间越界，所以概率向下偏。
\end{enumerate}

\section*{数值编程}
\begin{enumerate}[leftmargin=*,start=7]
  \item 对给定链，$P^5$ 给出 $(0.612500,0.387500)$，稳态分布为 $(0.600000,0.400000)$。交替链偶数步为 $(1,0)$、奇数步为 $(0,1)$，周期 2，直接反驳“有稳态便收敛”。条件结构反例中的条件概率分别为 $0.5$ 与 $1$。
  \item 率 2 的等待均值和方差为 $0.5,0.25$；率 2 与 3 叠加为 5；率 5 按 $0.3$ 保留后两流为 $1.5,3.5$；$\int_0^3(2+t)dt=10.5$。模拟中 Poisson 计数的均值与方差为 $5.980833$ 与 $5.988466$。
  \item Brownian 协方差最大误差为 $0.006948$。二次变差均值为 $1.000731$、方差 $0.007781$；绝对增量和解析期望为 $\sqrt{2n/\pi}$，总变差基线从 $3.191538$ 增至 $12.766153$。前两项模拟容差来自固定种子抽样误差，解析账本使用 $10^{-12}$ 绝对容差。
\end{enumerate}

\section*{研究判断}
\begin{enumerate}[leftmargin=*,start=10]
  \item 先按日内时段估计 $\lambda(t)$，检查计数均值/方差、等待时间指数性和增量自相关。季节性解释后若仍聚集，比较非齐次 Poisson、Cox/Hawkes 或状态模型的滚动样本外似然与残差；同时报告数据时间戳和合并事件规则。
  \item 滚动估计同一状态定义下的转移矩阵并检验稳定性；再把库存、波动、成交量或制度变量加入状态，检查给定扩充状态后的历史是否仍有预测力。若仍有，Markov 状态摘要不充分；若结构完整但矩阵漂移，则需时变转移或再训练协议。
  \item 明确 Brownian 连续路径、正态增量和连续时钟假设；量化跳跃风险、厚尾、离散监控误差、停牌/隔夜时间、参数漂移、流动性与执行误差。用跳扩散或经验压力场景做对照，不能把解析可解性当作现实正确性。
\end{enumerate}
