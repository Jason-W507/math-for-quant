# v0.5.1 — notebook 流程遗留清理版

本版本修正 notebook 从 Python 文本源迁移为直接跟踪 `.ipynb` 后仍留在书稿和工具中的旧流程说明：

- 答案册不再引用已删除的 `tools/check_learning_unit.py`，恢复路径直接运行对应的 notebook；
- 图形构建器按 `.ipynb` 查找教学 notebook，不再寻找已删除的 `.py` 文件；
- ADR、书稿说明和出版文档明确：发布只复制跟踪的 notebook，notebook 验证由修改者直接在 Jupyter 中完成。

发行资产：

- `output/pdf/math-for-quant-upper.pdf`
- `output/pdf/math-for-quant-lower.pdf`
- `output/pdf/math-for-quant-solutions.pdf`
- `output/math-for-quant-notebooks.zip`
