# ch09 fixture provenance

The fixture contains model inputs only. Expected values remain in `oracle.json`, which binds this file by SHA-256. The `provenance` field records the independent derivation used for the oracle.
