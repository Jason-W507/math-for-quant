# 符号与假设

资产简单收益记为 \(r_i\)，组合权重记为 \(w_i\)，组合收益记为 \(R_p=\sum_i w_i r_i\)。收益和权重均为无量纲量。核心命题讨论所有实数收益向量，并明确假设 \(\sum_i w_i=1\)；凸组合方向还要求 \(w_i\ge 0\)。

量纲检查中，用 \([q]=\text{share}\)、\([\Delta p]=\text{currency}/\text{share}\)，因此 \([q\Delta p]=\text{currency}\)。本章不把数学上的允许负权重等同于现实中无约束卖空。
